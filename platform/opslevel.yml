---
service:
  name: mlflow
  owner: data-science-platform
  tier: tier_3
  lifecycle: generally_available
  language: Python
  description: Open source platform for the machine learning lifecycle
  tags:
    - key: no-parameters
      value: true
    - key: no-tng-infrastructure
      value: true
    # The basic tag indicates to OpsLevel that this service is treated as a component.
    # A component is a service that does not really benefit from the OpsLevel rubric
    # or campaigns because it is (usually) developed by some other group.
    # Components are priced differently so please don't remove this unless the nature
    # of this service changes.
    - key: basic
      value: true
